// import { Card as Raw } from '../entities/card.entity';
// import { ColorsMapper } from './colors-mapper';
// import { Face } from '~/app/entities/face';

// export class FaceMapper {
//   static toDomain(raw: Raw) {
//     return new Face({
//       id: raw.id,
//       setId: raw.setId,
//       imageUri: raw.imageUri,
//       faceOfId: raw.id,
//       name: raw.name,
//       cmc: Number(raw.cmc),
//       language: raw.language,
//       manaCost: raw.manaCost,
//       effectText: raw.effectText,
//       flavorText: raw.flavorText,
//       typeLine: raw.typeLine,
//       createdAt: raw.createdAt,
//       updatedAt: raw.updatedAt,
//       colors: ColorsMapper.toDomain(raw.colors),
//     });
//   }
// }
